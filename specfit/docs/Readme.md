How to build the documentation with Sphinx

1) Install the required packages
   >>> python -m pip install -U sphinx
   >>> python -m pip install -U sphinx_rtd_theme
2) In the docs folder run the command
   >>> make html
